<style>
  .has-search .form-control {
    padding-left: 2.375rem;
  }

  .has-search .form-control-feedback {
    position: absolute;
    z-index: 2;
    display: block;
    width: 2.375rem;
    height: 2.375rem;
    line-height: 2.375rem;
    text-align: center;
    pointer-events: none;
    color: #0057B7;
  }

  #keyword-input {
    border-radius: 25px;
  }
</style>


<div class="container mt-4">
  <h5>Do not vote without fact</h5>
  <h6 class="text-muted mb-4">*서울의 경우 꼭 '구'와 '갑, 을, 병'까지 입력해주세요!</h6>
  지금까지 방문한 유권자 수: <%= Ahoy::Visit.count %>명
  <%= form_tag(root_path, method: :get, remote: true, class: "mt-3") do %>
    <div class="form-group has-search">
      <span class="fa fa-search form-control-feedback"></span>
      <input type="search" name="keyword" class="form-control border border-primary" placeholder="지역구 혹은 국회의원의 이름을 적으세요" id="keyword-input">
    </div>
    <button type="submit" class="btn btn vote-color text-white" style="width: 25%; background-color: #0057B7">팩트체크</button>
  <% end %>
  <div class="row" id="filter">
    <%= render 'home/district_filter', district: @district %>
    <%= render 'home/congress_filter', congress: @congress %>
  </div>
  <%# User.order(view: :desc).first(10).each do |user| %>
    <%#= user.name %>
  <%# end %>
</div>
